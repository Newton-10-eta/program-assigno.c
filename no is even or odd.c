// to find if a number is an even or odd
#include<stdio.h>
#include<conio.h>
int main()
{
    int a ,b;
    a =1;
    while (a<=b)
    {
        if(a%2==0)
            printf(" the number is an even number %d",a);
        else
            printf(" the number is an odd number %d", a);
    }
    return 0;
}
